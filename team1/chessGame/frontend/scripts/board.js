function renderChessBoard(board) {
    //render chess board 8x8
    //for {
        //render piece base on position
        //if else
        //0x0 -> black rook
    //}
}

function showValidateMove(piece) {
    //var result = validateMove(piece) ? true : false;
    //return result;
}

function validateMove(piece, from, to) {
    //if(piece === king) {
    //   if (check(piece)) {
    //      move king
    //   } 
    //}
    //else {
    // move piece
    //}
    return Boolean;
}

function showMoveHistory() {
    // show move history
}