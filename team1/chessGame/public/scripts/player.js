function resign(player, board) {
    //player -> lose
    //boardID -> showMoveHistory()
}

function timeOut() {
    //return result
}

function startGame(board) {
    renderChessBoard(board);
}

function movePiece(piece, from, to) {
    if (validateMove(piece, from, to)) {
        
    } 
}
